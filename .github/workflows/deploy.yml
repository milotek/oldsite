name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

# The default GITHUB_TOKEN is read-only for contents; Pages needs the rest.
permissions:
  contents: read
  pages: write
  id-token: write

# A queued deploy finishes rather than being cancelled, so main and the live
# site cannot end up disagreeing about which commit was last published.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run build
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - id: deploy
        uses: actions/deploy-pages@v4

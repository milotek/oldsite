<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>{{ site.title }}</title>
  <subtitle>{{ site.description }}</subtitle>
  <link href="{{ site.url }}"/>
  <link rel="self" href="{{ site.url }}feed.xml"/>
  <id>{{ site.url }}</id>
  <updated>{{ (posts[0].date if posts else year|string ~ '-01-01') }}T00:00:00Z</updated>
  <author><name>{{ site.name }}</name><email>{{ site.email }}</email></author>
  {% for post in posts %}
  <entry>
    <title>{{ post.title }}</title>
    <link href="{{ site.url }}{{ post.url }}"/>
    <id>{{ site.url }}{{ post.url }}</id>
    <published>{{ post.date }}T00:00:00Z</published>
    <updated>{{ post.date }}T00:00:00Z</updated>
    {% if post.summary %}<summary>{{ post.summary }}</summary>{% endif %}
    <content type="html">{{ post.body | replace('href="/', 'href="' ~ site.url) | replace('src="/', 'src="' ~ site.url) }}</content>
  </entry>
  {% endfor %}
</feed>

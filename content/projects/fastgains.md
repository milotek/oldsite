+++
title   = "fastgains"
group   = "apps"
year    = "2024 - 2025"
role    = "solo"
tagline = "A calorie tracker. Swift first, then rewritten in React Native."
stack   = ["Swift", "SwiftUI", "React Native", "Supabase", "PostgreSQL"]
order   = 25
slate   = "swift then RN"
+++

Log food fast, see what it adds up to, stop thinking about it. The whole design goal was fewer taps per meal.

Started as SwiftUI for iOS, then rewritten in React Native in 2025 to cover the other platforms, with a self-hosted Supabase instance behind it.

Repo is private. The screenshots from the old site were placeholders, so I'd rather show nothing than show those.
